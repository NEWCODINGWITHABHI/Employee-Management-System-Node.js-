<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        table thead{
            display: inline-block;
          
            background-color: black;
            color: white;
            display: flex;
            justify-content: space-between;
        }
        table thead th{
           
            margin-left: 20px !important;
        }
          table tbody{
            display: block;
            background-color: black;
            color: aliceblue;
        }
        table th{
            font-size: 30px;
            margin: 20px;
            padding: 20px;
        }
         table td{
            font-size: 20px;
            padding: 30px;
            text-align: left;
         }
    </style>
</head>
<body>
    <h1>Welcome to Dashboard</h1>
     <h3><a href="/admin/add-new-user">Add New User</a></h3>
    <h2>Users Data</h2>
    <table class="user-table-list">
        <thead>
            <th>Name</th>
            <th>Email</th>
            <th>Mobile</th>
            <th>Verified</th>
            <th>Action</th>
        </thead>
        <tbody>
          <%  if(users.length>0){  %>
               <% for(let i=0;i<users.length;i++){  %>
                    <tr>
                        <td><%= users[i].name %></td>
                        <td><%= users[i].email %></td>
                        <td><%= users[i].mobileno %></td>
                        <td>
                           <% if(users[i].isVerified==1){ %>
                              verified
                          <%  }
                         else{ %>
                                Not Verified
                             
                           <% } %>
                        </td>
                        <td><a href="/admin/edit-user?id=<%= users[i]._id %>" >Edit </a></td>
                        <td><a href="/admin/delete-user?id=<%= users[i]._id %>" >Delete </a></td>
                    </tr>

               <% } %>

          <%  }
           else{ %>
           <td>
            <h2>Users Does not exist</h2>
           </td>
         <%  } %>
        </tbody>
    </table>

    <a href="/admin/home">Go to Home</a>
</body>
</html>